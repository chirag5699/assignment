#include <stdio.h>
int main()
{
    int a, b, c;
    printf("enter three namber");
    scanf("%d,%d,%d", a, b, c);

    if (a > b)

        if (a > c)

        {

            printf("a is bigist");
        }

        else 
        {
            if(b>c)

                printf("b is bigist");
                printf("c is bigist");
        }

            return 0;
}